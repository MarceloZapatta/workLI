# workLI
Superfast your productivity

## How to build

This project is using docker so you can build using the command below:

```bash
docker build -t workli .
```

To run the application you can use:

```bash
docker run -it workli {ARGS}
```
