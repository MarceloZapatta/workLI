{
  "version": "0.0.1",
  "compilerOptions": {
    "lib": ["deno.ns", "dom"]
  },
  "tasks": {
    "start": "deno run --allow-read --allow-write --allow-run --allow-env index.ts",
    "build": "deno compile -o dist/workli --allow-read --allow-write --allow-run --allow-env index.ts"
  }
}
